{
  "name": "model-workshop-manager",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-level tooling only — apps/backend and apps/frontend remain independently installed, see AGENTS.md",
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.9.0",
    "oxlint": "^1.81.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json}": [
      "oxfmt",
      "oxlint"
    ]
  }
}
